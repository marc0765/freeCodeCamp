# Build a Report Card Printer
# This program demonstrates different Python data types (string, boolean, integer, and float) and checks their values and types.

name = 'Alice'
print(name, type(name))

is_student = True
print(is_student, type(is_student))

age = 20
print(age, type(age))

score = 80.5
print(isinstance(score, float))
print(score, type(score))
